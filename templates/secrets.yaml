apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mattermost-stack.fullname" . }}-secrets
  labels:
    {{- include "mattermost-stack.labels" . | nindent 4 }}
type: Opaque
stringData:
  POSTGRES_PASSWORD: "HnlRbZOMPrJCgIQJwJ6vQqFo"
  MATTERMOST_BOT_TOKENS: "{{ join "," (index .Values "ollama-bridge").botTokens }}"
